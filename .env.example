# DB에 없는 약물에 대한 AI 분석을 활성화하려면 Gemini API 키를 입력하세요.
# 이 파일을 .env.local로 복사한 뒤 값을 채워 사용합니다.
# 실제 값이 들어간 .env / .env.local은 절대로 커밋하지 마세요 (.gitignore에 포함됨)

GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-1.5-flash
